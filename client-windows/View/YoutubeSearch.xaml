<UserControl x:Class="client.View.YoutubeSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:client.View"
             xmlns:vm="clr-namespace:client.ViewModel"
             xmlns:controllers="clr-namespace:client.UI.Styles.Controllers"
             mc:Ignorable="d" 
             Background="#000"
             d:DesignHeight="700" d:DesignWidth="775">

    <UserControl.Resources>
        <controllers:SelectedItemConvertor x:Key="SelectedItemConvertor" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:YoutubeSearchVM />
    </UserControl.DataContext>
    
    <Border CornerRadius="10" Background="#111" Margin="10 0" Padding="10"
            BorderThickness="1" BorderBrush="{StaticResource ColorBorder}" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
                <RowDefinition Height="auto" />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <Label Content="Youtube Filter" Style="{StaticResource LabelDefault_Style}" FontSize="25" />
                <Border Background="#333" Height="1" Margin="0 10" />
            </StackPanel>

            <TextBox Grid.Row="1" Background="Transparent" BorderThickness="1" BorderBrush="#333" Text="Search"
                     Foreground="#999" Style="{StaticResource Font}" Padding="10" FontSize="17">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10" />
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <StackPanel Grid.Row="2" Height="auto">
                <StackPanel>
                    <Label Content="Category" Style="{StaticResource LabelDefault_Style}" FontSize="18" />
                    <Border Background="#333" Height="1" Margin="0 10" />
                </StackPanel>

                <WrapPanel>
                    <ItemsControl ItemsSource="{Binding Categoryes}" x:Name="categoryItems">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{Binding IsSelected, Converter={StaticResource SelectedItemConvertor}}" BorderThickness="1" BorderBrush="#444" 
                                        CornerRadius="16" Padding="10 0" Margin="5 5" Cursor="Hand" IsHitTestVisible="True">
                                    <Border.InputBindings>
                                        <MouseBinding Gesture="LeftClick"
                                                      Command="{Binding ElementName=categoryItems, Path=DataContext.CategoryClickCommand}" 
                                                      CommandParameter="{Binding .}" />
                                    </Border.InputBindings>
                                    <StackPanel Orientation="Horizontal" IsHitTestVisible="True">
                                        <Ellipse Width="15" Height="15" StrokeThickness="2" Stroke="#999" IsHitTestVisible="False" />
                                        <Label Content="{Binding Text}" Style="{StaticResource LabelDefault_Style}" FontSize="14" Margin="5 0 0 0" IsHitTestVisible="False" x:Name="LabelCategory" />
                                    </StackPanel>
                                    <Border.Triggers>
                                        <EventTrigger RoutedEvent="MouseEnter">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#fff" Duration="0:0:0.4" />
                                                    <ColorAnimation Storyboard.TargetName="LabelCategory" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)" To="#000" Duration="0:0:0.35" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MouseLeave">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#111" Duration="0:0:0.25" />
                                                    <ColorAnimation Storyboard.TargetName="LabelCategory" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)" To="#fff" Duration="0:0:0.2" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Border.Triggers>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </WrapPanel>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <StackPanel>
                    <Label Content="The year of the video" Style="{StaticResource LabelDefault_Style}" FontSize="18" />
                    <Border Background="#333" Height="1" Margin="0 10" />
                </StackPanel>

                <TextBox Background="Transparent" BorderThickness="1" BorderBrush="#333" Text="Year"
                         Style="{StaticResource Font}" Foreground="#999" Padding="10" FontSize="17">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10" />
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </StackPanel>

            <StackPanel Grid.Row="4" VerticalAlignment="Bottom">
                <Grid>
                    <Border Background="{StaticResource ColorRed}" CornerRadius="7" Padding="8" Cursor="Hand">
                        <Label Content="SEARCH" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                Style="{StaticResource LabelDefault_Style}" FontSize="15" Margin="0 -4 0 0" />

                        <Border.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#940000" Duration="0:0:0.08" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FFFF2400" Duration="0:0:0.03" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                    </Border>
                    <Border Background="#000" Opacity="0.4" Height="9" VerticalAlignment="Bottom" />
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
